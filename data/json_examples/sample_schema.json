{
  "type": "object",
  "properties": {
    "overall_score": {
      "type": "number",
      "minimum": 0,
      "maximum": 100,
      "description": "A holistic score representing resume quality."
    },
    "suggestions": {
      "type": "array",
      "description": "List of specific edit suggestions with precise character offsets.",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Unique identifier for the suggestion."
          },
          "start_char": {
            "type": "integer",
            "description": "Start character offset (0-indexed) in the input resume text."
          },
          "end_char": {
            "type": "integer",
            "description": "End character offset (0-indexed, inclusive) in the input resume text."
          },
          "line_number": {
            "type": "integer",
            "description": "Line number (1-indexed) where the text occurs."
          },
          "original_text": {
            "type": "string",
            "description": "Exact substring from the input resume that is being improved."
          },
          "suggested_text": {
            "type": "string",
            "description": "Model's improved text to replace the original substring."
          },
          "explanation": {
            "type": "string",
            "description": "Reason for the suggested change."
          },
          "category": {
            "type": "string",
            "description": "Type of issue addressed: grammar, clarity, impact, quantification, formatting, or length."
          },
          "confidence": {
            "type": "number",
            "description": "Model's confidence (0–1)."
          }
        },
        "required": [
          "id",
          "start_char",
          "end_char",
          "original_text",
          "suggested_text",
          "explanation"
        ]
      }
    },
    "high_level_rewrite": {
      "type": "string",
      "description": "A full resume rewritten with all suggestions applied."
    }
  },
  "required": ["suggestions"]
}
